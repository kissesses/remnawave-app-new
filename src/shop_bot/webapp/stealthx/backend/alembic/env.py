"""Alembic migrations for STEALTHX tables.

Primary schema is managed via schema_postgres.py on app startup.
Use Alembic for incremental migrations in development:

    alembic -c src/shop_bot/webapp/stealthx/backend/alembic.ini upgrade head
"""
